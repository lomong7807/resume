---
import { profile } from '../data/profile';
import { withBase } from '../lib/url';

const home = withBase('');
const sections = [
  { id: 'about', label: '소개' },
  { id: 'skills', label: '기술' },
  { id: 'experience', label: '경력' },
  { id: 'projects', label: '프로젝트' },
  { id: 'education', label: '학력·활동' },
];
const careerPath = withBase('career/');
const onCareer = Astro.url.pathname.replace(/\/$/, '') === careerPath.replace(/\/$/, '');
---

<nav class="site-nav" aria-label="섹션 이동">
  <div class="container nav-inner">
    <a class="brand" href={home}>{profile.name}</a>
    <ul class="nav-links">
      {sections.map((section) => (
        <li><a href={`${home}#${section.id}`}>{section.label}</a></li>
      ))}
      <li>
        <a href={careerPath} class:list={[{ 'is-active': onCareer }]}>경력기술서</a>
      </li>
    </ul>
  </div>
</nav>

<style>
  .site-nav {
    position: sticky;
    top: 0;
    z-index: 10;
    background: color-mix(in srgb, var(--bg) 85%, transparent);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border-bottom: 1px solid var(--line);
  }

  .nav-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.5rem;
    height: 3.5rem;
  }

  .brand {
    font-weight: 700;
    font-size: 1rem;
    letter-spacing: -0.01em;
    color: var(--ink);
    flex-shrink: 0;
  }

  .brand:hover {
    text-decoration: none;
  }

  .nav-links {
    display: flex;
    gap: 1.4rem;
    overflow-x: auto;
    scrollbar-width: none;
  }

  .nav-links::-webkit-scrollbar {
    display: none;
  }

  .nav-links a {
    display: block;
    padding-block: 0.35rem;
    font-size: 0.88rem;
    font-weight: 500;
    color: var(--ink-muted);
    white-space: nowrap;
    border-bottom: 2px solid transparent;
    transition: color 150ms ease, border-color 150ms ease;
  }

  .nav-links a:hover {
    color: var(--ink);
    text-decoration: none;
  }

  .nav-links a.is-active {
    color: var(--ink);
    border-bottom-color: var(--accent);
  }

  @media (max-width: 760px) {
    .nav-links {
      gap: 1rem;
    }
  }
</style>
